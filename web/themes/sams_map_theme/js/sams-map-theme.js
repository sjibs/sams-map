/**
 * @file
 * sams-map-theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.samsMapTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
